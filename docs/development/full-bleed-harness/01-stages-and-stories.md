# 阶段与原子用户故事：Harness 去外壳化

## 阶段与子任务

| 阶段 | 子任务 | 交付结果 | 原子任务 | 用户故事 | 状态 |
| --- | --- | --- | --- | --- | --- |
| S1 | ST-201 | 单层界面契约 | AT-201 | US-201 | 已完成 |
| S2 | ST-202 | 全窗运行态 | AT-202 | US-202 | 已完成 |
| S2 | ST-203 | 嵌入式余额入口 | AT-203 | US-203 | 已完成 |
| S3 | ST-204 | 原生菜单入口 | AT-204 | US-204 | 已完成 |
| S3 | ST-205 | 构建、截图与审核 | AT-205 | US-205 | 已完成 |
| S4 | ST-206 | 余额面板收起行为 | AT-206 | US-206 | 已完成 |
| S4 | ST-207 | API Key 配置主入口 | AT-207 | US-207 | 已完成 |
| S5 | ST-208 | 余额与官方侧栏联动 | AT-208 | US-208 | 已完成 |
| S6 | ST-209 | 折叠态像素对齐 | AT-209 | US-209 | 已完成 |
| S7 | ST-210 | 展开态轻量侧栏行 | AT-210 | US-210 | 已完成 |
| S8 | ST-211 | 公开发布安全收口 | AT-211 | US-211 | 已完成 |
| S9 | ST-212 | 展开态图标网格对齐 | AT-212 | US-212 | 已完成 |
| S10 | ST-213 | 欢迎页入口文案精简 | AT-213 | US-213 | 已完成 |
| S11 | ST-214 | 应用默认工作区 | AT-214 | US-214 | 已完成 |
| S12 | ST-215 | 顶部模式切换器优化 | AT-215 | US-215 | 已完成 |
| S13 | ST-216 | 折叠栏自定义图标统一 | AT-216 | US-216 | 已完成 |
| S14 | ST-217 | API 峰谷计价展示 | AT-217 | US-217 | 已完成 |
| S15 | ST-218 | 余额浮层避让主题入口 | AT-218 | US-218 | 已完成 |
| S16 | ST-219 | 计价信息渐进展示 | AT-219 | US-219 | 已完成 |
| S17 | ST-220 | 官方人民币价格展示 | AT-220 | US-220 | 已完成 |
| S18 | ST-221 | 当前会话 token 用量 | AT-221 | US-221 | 已完成 |
| S19 | ST-222 | API Key 并入应用设置 | AT-222 | US-222 | 已完成 |
| S20 | ST-223 | 已配置凭据的设置页精简 | AT-223 | US-223 | 已完成 |
| S21 | ST-224 | Harness 设置新手引导 | AT-224 | US-224 | 已完成 |
| S22 | ST-225 | 主题背景并入应用设置 | AT-225 | US-225 | 已完成 |

## AT-201：单层界面契约

用户故事 US-201：作为用户，我希望窗口看起来就是 Harness，而不是一个桌面壳里再打开一个网页应用。

### 验收标准

- Given Harness 已运行，When 窗口显示，Then 看不到 DS Harness 原生侧栏、项目卡片和原生顶部项目栏。
- Given Harness 未就绪，When 启动或失败，Then 原生状态页仍能解释原因。

## AT-202：全窗 WebView

用户故事 US-202：作为 Harness 用户，我希望官方界面占满可用空间，以便获得与网页版一致的视觉层级。

### 验收标准

- WebView 是运行态根内容，四边直接贴合窗口内容区。
- SwiftUI 状态更新不会重新创建 WKWebView 或丢失当前会话。

### 验证方式

- release 构建；实机截图；交互检查。

## AT-203：嵌入式余额

用户故事 US-203：作为 API 用户，我希望余额仍然可见，但它应属于 Harness 左侧栏，而不是另起一层外壳。

### 验收标准

- 余额胶囊位于 Harness 左侧栏底部、设置项上方。
- 未配置、加载、成功、失败四态均有短文本和状态点。
- 点击余额胶囊只向原生层发送固定 `open` 消息，不携带 API Key 或余额 JSON。

### 验证方式

- 代码审核；WKWebView 实机交互。

## AT-204：原生操作可达

用户故事 US-204：作为桌面用户，我希望移除外层控件后仍能切换项目、查看日志和配置余额。

### 验收标准

- macOS 菜单提供选择项目、新任务、刷新、余额设置和日志。
- 不向 Web UI 叠加新的原生导航栏。

## AT-205：完工验证

用户故事 US-205：作为产品负责人，我希望用真实运行截图确认只剩一层主界面。

### 验收标准

- checks、build、plist 和 codesign 通过。
- 截图展示单层 Harness 和左侧栏余额入口。

## AT-206：明确收起余额明细

用户故事 US-206：作为用户，我希望展开余额后能明确关闭，并在执行设置或刷新后自动收起，以免明细一直遮挡左侧栏。

### 验收标准

- 明细右上角显示独立关闭按钮。
- 再次点击余额摘要、点击明细外部或点击关闭按钮均可收起。
- 点击配置 API Key 或刷新后，明细立即收起。
- 每次页面重新载入时默认保持收起。

## AT-207：直观配置 API Key

用户故事 US-207：作为用户，我希望余额明细中直接看到“配置 API Key”，以便无需寻找系统菜单。

### 验收标准

- 明细底部提供文字完整的“配置 API Key”主按钮。
- 点击后打开现有原生安全配置页，使用 SecureField 和 macOS Keychain。
- 已保存 Key 不回显；保存后自动刷新余额。
- 即使存在 `DEEPSEEK_API_KEY` 环境变量也显示输入框；钥匙串凭据优先，移除后回退到环境变量。

## AT-208：余额跟随官方侧栏折叠

用户故事 US-208：作为使用紧凑 Harness 侧栏的用户，我希望余额入口同步缩成图标，以免折叠侧栏后仍有一张宽卡片遮挡主界面。

### 验收标准

- Given 官方侧栏展开，Then 余额显示状态点、金额和说明。
- Given 官方侧栏折叠，Then 余额入口缩为与侧栏图标一致的 40×40pt 按钮，只显示钱包图标与状态点。
- When 从折叠切换为展开或反向切换，Then 余额样式在同一页面内自动更新，不重载会话。
- Given 紧凑余额按钮，When 点击，Then 仍可展开余额详情和进入 API Key 配置。

### 实现约束

- 优先通过官方侧栏开关的可访问名称判断，不依赖压缩 CSS 类名。
- 使用 MutationObserver 监听按钮属性变化，并限制观察范围与更新频率。

## AT-209：紧凑余额图标中心线对齐

用户故事 US-209：作为重视界面一致性的用户，我希望余额图标与官方侧栏图标居中对齐，以便折叠栏看起来属于同一套设计。

### 验收标准

- Given 官方折叠侧栏约 52pt 宽、余额按钮 40pt 宽，Then 紧凑态使用 6pt 左边距。
- Given 用户局部截图，When 对比鲸鱼、新会话、搜索、余额和设置，Then 图标视觉中心位于同一条竖直中心线，误差不超过 2px。
- 展开态继续使用 12pt 左边距，不受该修正影响。

## AT-210：展开态轻量侧栏行

用户故事 US-210：作为使用展开侧栏的用户，我希望余额入口和“设置”属于同一套菜单样式，而不是一张单独悬浮的卡片。

### 验收标准

- Given 官方侧栏宽度可能随窗口变化，Then 余额入口动态使用“侧栏宽度减 24pt”，始终保留 12pt 两侧留白。
- Given 余额入口未悬停，Then 不显示独立边框、实体背景、阴影或模糊材质。
- When 指针悬停，Then 只显示与官方菜单一致的轻微圆角底色。
- 状态点、金额和说明与下方设置行对齐；余额详情仍使用独立浮层，折叠态继续保持 6pt 左边距和 40×40pt 尺寸。

## AT-211：公开发布安全收口

用户故事 US-211：作为发布者，我希望源码公开时不会误带本地缓存或扩大 API Key 暴露面，并能明确知道正式分发前的剩余风险。

### 验收标准

- `.smoke-npm-cache/` 与 `.test-dsh-home/` 被 Git 忽略，普通 `git add .` 不会收录本地测试状态或 npm 日志。
- `DEEPSEEK_API_KEY` 只供原生余额客户端使用，不继承给 `npx`/Harness 子进程。
- 发布审核记录无硬编码密钥命中，并明确列出截图隐私、许可证、品牌、版本锁定、Developer ID 签名与公证风险。

## AT-212：统一余额与设置图标网格

用户故事 US-212：作为使用展开侧栏的用户，我希望余额和设置使用相同的图标列与文字起点，而不是用小状态点假装成菜单图标。

### 验收标准

- 余额使用固定 18pt 的 `¥` 图标槽，图标中心与设置齿轮中心位于同一竖线。
- 状态点缩为图标右上角徽标，不再独占一列或影响文字布局。
- 展开态图标左缘约为 16pt、中心约为 25pt，金额文字起点约为 39pt，与设置行误差不超过 1px。
- 折叠态仍保持 40×40pt 点击区和约 26pt 的整栏中心，不改变详情与 API Key 交互。

## AT-213：精简欢迎页入口

用户故事 US-213：作为首次进入 Harness 的用户，我希望主按钮只显示“进入”，以便界面更简洁。

### 验收标准

- 欢迎页主按钮显示“进入”。
- “进入”按钮使用简洁文案与直接进入图标。
- 菜单中的“选择项目…”及 `⌘O` 保持不变。

## AT-214：无需选择文件夹直接进入

用户故事 US-214：作为只想立即体验 Harness 的用户，我希望点击“进入”后直接启动，而不是先选择文件夹。

### 验收标准

- 点击“进入”不会打开 `NSOpenPanel`。
- 首次进入时自动创建并使用 `Application Support/app.dsharness.desktop/Workspace`，不授权整个个人目录。
- 默认工作区会作为最近工作区保存，后续启动自动恢复。
- 菜单“选择项目…”和 `⌘O` 继续用于切换到用户自己的代码目录。
- 默认目录创建失败时显示明确错误，不静默停留在欢迎页。

## AT-215：优化顶部模式切换器

用户故事 US-215：作为频繁切换工作与聊天的用户，我希望 Harness 和 Chat 在顶部控件中等宽、对齐且状态明确，以便快速判断当前界面。

### 验收标准

- Harness 与 Chat 占用相同宽度，文字和图标分别在各自点击区域内居中。
- 控件宽度比原生分段控件更紧凑，不出现两侧大段空白。
- 选中态使用克制的品牌强调色，未选中态保持清晰但不抢占视觉焦点。
- 点击两个选项仍调用现有 `selectSurface`，菜单快捷键和 WebView 保活行为不变。

## AT-216：统一折叠栏自定义图标

用户故事 US-216：作为使用折叠侧栏的用户，我希望主题与余额图标和官方新会话、项目、搜索及设置图标具有一致的视觉尺寸和中心线，以便整列图标看起来属于同一套工具栏。

### 验收标准

- 主题与余额继续使用 40×40pt 点击区，图标内容统一放入 24×24pt 视觉画布。
- 主题图片图标的可见轮廓放大到接近官方设置图标；余额使用带 `¥` 的 18pt 圆形币种轮廓并保持状态徽标可见。
- 两个自定义图标的视觉中心相对当前紧凑态右移约 2pt，与官方图标中心线误差不超过 1pt。
- 调整仅作用于折叠态；展开态图标列、文字起点、余额面板和主题设置交互保持不变。

## AT-217：展示 API 峰谷时段与模型价格

用户故事 US-217：作为 DeepSeek API 用户，我希望在余额入口中看到当前处于高峰还是谷时、下一次切换时间，以及各模型的两档单价，以便选择更合适的调用时间和模型。

### 规则基线（由 AT-220 更新价格币种与模型范围）

- 官方中文表价格单位为人民币 / 1M tokens。
- 高峰为周一至周五 UTC 01:00–04:00、06:00–10:00，对应北京时间 09:00–12:00、14:00–18:00；其他时间均为谷时。
- 谷时价格为高峰的 50%。
- 模型范围：`deepseek-v4-flash`、`deepseek-v4-pro`。
- 事实来源：DeepSeek 中文官方 `https://api-docs.deepseek.com/zh-cn/quick_start/pricing/`，2026-08-24 查询。

### 验收标准

- 余额侧栏摘要和展开面板明确显示“当前高峰”或“当前谷时”。
- 展开面板显示北京时间峰谷规则和下一次切换到另一时段的日期、时间。
- 每个模型同时列出高峰/谷时的缓存命中输入、缓存未命中输入和输出单价，当前计价行高亮。
- 时段判断使用 UTC 固定规则，不受 Mac 当前系统时区变化影响；显示时间明确标注为北京时间。
- 时段状态无需重载 Harness 即可自动更新，最大延迟不超过 60 秒。
- 面板提供官方价格页入口，并注明价格可能由 DeepSeek 调整。

## AT-218：余额浮层与主题入口互不冲突

用户故事 US-218：作为展开余额面板的用户，我希望配置与刷新操作完整显示并可点击，不被后面的主题背景入口覆盖。

### 验收标准

- 展开态余额面板底边位于主题背景与余额摘要两行之上，不发生文字或点击区域重叠。
- 折叠侧栏时使用对应的紧凑行高避让，浮层仍保持在可视区域内并可滚动。
- 浮层拥有高于侧栏入口的绘制与命中层级；面板内点击不会误触主题背景。
- 点击浮层外部仍可收起面板，收起后主题背景入口保持可用。

## AT-219：精简计价面板并突出当前时段

用户故事 US-219：作为查看余额的用户，我希望第一眼就看清当前是高峰期还是谷时，同时不被整页模型价目表干扰。

### 验收标准

- 展开侧栏时，金额同一行显示醒目的“高峰期”或“谷时”彩色标签。
- 余额面板标题区再次显示当前时段标签，下方只保留下次切换时间与简短规则。
- 两款模型的峰谷双档价格仍可查看，但默认收在“查看模型价格”折叠区内。
- 默认展开余额面板时，无需滚动即可看到余额、当前时段、配置 API Key 和刷新按钮。
- 折叠侧栏仍只显示统一尺寸图标，不增加文字拥挤。

## AT-220：直接使用中文官方人民币单价

用户故事 US-220：作为使用人民币充值的用户，我希望模型单价以人民币显示，以便直接估算调用成本。

### 价格基线

- 事实来源使用用户指定的 DeepSeek 中文官方价格表：`https://api-docs.deepseek.com/zh-cn/quick_start/pricing/`。
- 中文表列出的标准价作为高峰价；现有谷时五折规则继续生成谷时价。
- 模型范围与中文表一致，只展示 `deepseek-v4-flash` 和 `deepseek-v4-pro`。

### 验收标准

- 模型价格折叠区单位显示“人民币 / 百万 tokens”，不再显示美元单位。
- 所有缓存命中、缓存未命中和输出价格以 `¥` 开头，保留官方价格所需精度。
- 价格来源入口打开 DeepSeek 中文价格表，不显示美元符号或汇率说明。
- 余额金额继续使用 API 返回的 CNY 数据，不重复换算。

## AT-221：展示当前 Harness 会话已用 tokens

用户故事 US-221：作为正在使用 Harness 的用户，我希望在余额区域看到当前会话已经消耗的输入和输出 tokens，以便掌握用量。

### 数据边界

- DeepSeek 余额接口不提供账号累计 token 用量；官方文档只在每次模型响应的 `usage` 中返回用量。
- Harness 已将请求 usage 汇总为当前会话的输入与输出 tokens，因此应用只读取同一页面上的本地统计文本。

### 验收标准

- 侧栏余额第二行显示当前会话“输入 X · 输出 Y”；余额状态仍由颜色点表示。
- 展开余额面板显示“当前会话 tokens”及输入、输出两个值。
- 会话运行时用量随 Harness 统计文本变化自动刷新，无需重载页面。
- 不依赖 Harness 压缩类名；兼容中文“输入/输出”和英文“Input/Output”统计文本。
- 没有会话用量时明确显示“暂无用量”，不估算、不读取其他会话或账号历史。

## AT-222：将余额 API Key 配置并入原生设置

用户故事 US-222：作为 macOS 用户，我希望 DeepSeek API Key 在应用“设置”中配置，而不是每次打开一个独立完成弹窗，以便设置入口和交互符合平台习惯。

### 安全边界

- Harness 左下角“设置”属于官方 Web UI；余额 Key 不注入该页面，避免凭据进入 WebView DOM 或 JavaScript 上下文。
- 应用使用原生 SwiftUI `Settings` 场景承载“API 与余额”，继续通过 `SecureField` 输入并保存到 macOS 钥匙串。

### 验收标准

- 余额面板“配置 API Key”、旧余额入口及应用菜单均打开同一个原生设置窗口。
- 设置窗口标题为“设置”，API Key 位于“API 与余额”分区，不再显示独立弹窗的“完成”按钮。
- 保存成功后设置窗口保持打开，并刷新余额；现有 Key 不回显，移除功能保持可用。
- Key 不进入 Harness 页面、项目文件、日志或 Harness 子进程环境。

## AT-223：已配置时隐藏 API Key 输入框

用户故事 US-223：作为已经配置余额凭据的用户，我希望设置页只显示“已配置”状态，不必每次看到一个空 API Key 输入框。

### 验收标准

- 凭据来源为环境变量或钥匙串时，默认不渲染 `SecureField` 和“保存并刷新”按钮。
- 已配置状态明确说明当前使用环境变量或 macOS 钥匙串，并继续提供“刷新余额”。
- 用户点击“更换 API Key”后才显示空的安全输入框、取消更换和保存动作；现有 Key 永不回显。
- 未配置凭据时继续直接显示安全输入框和“保存并刷新”。
- 钥匙串来源仍可移除；移除后若没有环境变量，则设置页回到未配置输入状态。

## AT-224：为 Harness 官方设置页增加新手引导

用户故事 US-224：作为第一次使用 Harness 的用户，我希望设置页用简短中文解释每一类设置及推荐选择，以便在不了解术语时也能安全开始使用。

### 范围

- 通过现有 WKWebView user script 增加只读引导层，不修改 Harness 上游包或设置值。
- 引导覆盖通用设置、会话预设、权限、语言与外观、繁忙时发送行为，以及模型/插件/Agent 预设入口。

### 验收标准

- 首次检测到 Harness 设置页时自动展示一次分步引导；关闭或完成后不在后续每次打开时打扰用户。
- 设置页顶部显示“新手引导”按钮，用户随时可以重新打开。
- 每一步包含“这是做什么的”“不确定时怎么选”和必要的风险提示；权限步骤明确推荐 `Workspace Write` 作为日常项目工作的平衡选项。
- 引导高亮当前讲解区域，支持上一步、下一步、跳过/完成和 Escape 关闭。
- 引导层不读取、保存或改写任何 Harness 设置；关闭后原页面操作恢复正常。
- 上游文字或结构轻微变化导致目标未找到时，引导卡仍可完整阅读，不阻塞设置页。

### 验证方式

- release 构建；实机首次打开、重复打开、前后翻页与关闭检查；检查关键引导文案和设置值不变。

## AT-225：将主题背景迁入统一设置窗口

用户故事 US-225：作为使用主题图片的用户，我希望主题背景与 API/余额放在同一个应用设置窗口中，以便从一个地方管理本机配置。

### 范围

- 应用设置增加“API 与余额”和“主题背景”两个栏目。
- 复用现有主题预览、选择/更换图片、移除背景、遮罩滑杆、错误与隐私说明。
- 移除主题背景独立 sheet；Harness 侧栏、应用菜单和 `⇧⌘T` 统一请求打开设置中的主题栏目。

### 验收标准

- 设置窗口左侧可在“API 与余额”和“主题背景”之间切换，选中态清晰。
- 余额入口与 `⇧⌘B` 打开设置时选中“API 与余额”；主题入口与 `⇧⌘T` 打开时选中“主题背景”。
- 已存在的背景图片与遮罩比例完整显示；选择、更换、移除和滑杆即时生效。
- 不再显示主题背景独立弹窗及“完成”按钮；关闭行为由 macOS 设置窗口统一管理。
- 图片仍只保存于本机 Application Support，不改变存储、网络或工作区边界。

### 验证方式

- release 构建；实机检查三个主题入口、栏目切换、当前图片预览和遮罩调节。
