# Codex 5.5 审核报告：Harness 去外壳化

## 审核范围

- `ContentView.swift` 运行态条件分支和全窗 WebView。
- `HarnessWebView.swift` 余额 user script、状态更新与受限消息桥。
- `DeepSeekChatWebView.swift` 与 `DeepSeekChatEnterBehavior.swift` 的全部可编辑控件回车脚本及其输入法、修饰键和非编辑控件边界。
- `AppModel.swift` 非敏感余额展示模型；`HarnessDockApp.swift` 菜单入口。
- release 构建、真实 Harness 会话、余额胶囊和明细面板。

## 问题发现

| 严重级别 | 问题 | 证据 | 必要动作 |
| --- | --- | --- | --- |
| - | 运行态功能未发现 P0/P1/P2 缺陷 | 实机只有官方 Harness 一层主界面；余额桥不传输凭据 | 无 |
| P3 | 旧的原生侧栏组件代码仍保留但不再进入运行态 | `ContentView` 根分支不引用 `SidebarView`/`WorkspaceView` | 后续代码整理时删除，不影响当前交付 |
| P1 | 公开发布包尚不满足授权和分发门禁 | SF Symbols AppIcon、ad-hoc 签名、arm64 单架构、未锁上游版本 | 见 `docs/release-readiness.md`，正式发布前处理 |

## 验收标准检查

| 用户故事 | 结果 | 证据 |
| --- | --- | --- |
| US-201 | 通过 | 实机截图不再显示外层 HarnessDock 侧栏和项目顶栏 |
| US-202 | 通过 | AX tree 仅一个 Harness HTML 内容区；会话保持 |
| US-203 | 通过 | 左侧栏显示真实 `¥7.12`，明细显示赠送/充值/更新时间 |
| US-204 | 通过 | macOS 菜单保留项目、新任务、刷新、余额设置和日志 |
| US-205 | 通过 | checks/build/plist/codesign 与实机截图完成 |
| US-206 | 通过 | AX tree 明确显示 expanded/collapsed 状态和“收起余额明细”按钮 |
| US-207 | 通过 | 明细含“配置 API Key”；原生页包含 SecureField 和“保存并刷新” |
| US-208 | 通过 | 官方按钮显示“打开侧边栏”时，余额实机缩为 40pt `¥` 图标；脚本不重载页面 |
| US-209 | 通过 | 折叠态使用 6pt 左边距，实机截图中余额与官方图标中心线一致；展开态仍为 12pt |
| US-210 | 通过 | 展开态改为动态宽度无框侧栏行，状态点、文字与设置行实机对齐；详情仍为浮层 |
| US-211 | 通过 | 子进程环境移除余额 Key；本地缓存和测试 Home 已忽略；发布阻塞项完成分级审计 |
| US-212 | 通过 | 展开态使用固定 18pt `¥` 图标槽，绿点改为右上角徽标；图标中心和文字起点均与设置行实机对齐 |
| US-213 | 通过 | 独立测试副本显示“进入”，欢迎页说明与图标均使用直接进入语义 |
| US-214 | 通过 | 清除临时测试偏好后点击“进入”，未出现目录选择器并直接显示 Harness；默认工作区已创建和保存 |
| US-215 | 通过 | 自定义切换器为两个等宽按钮；实机完成 Harness → Chat → Harness 双向切换，选中状态可访问值同步更新 |
| US-216 | 通过 | 折叠态实机截图中主题、余额币种圆标和设置齿轮中心同轴，可见轮廓尺寸接近；状态徽标保持可见 |
| US-217 | 通过 | AX tree 和实机面板显示当前高峰、下一切换时间、北京时间规则及模型峰谷双档价格；当前高峰行已高亮 |
| US-218 | 通过 | 实机面板底边位于主题入口上方；刷新后只收起余额面板，主题设置未误开且随后仍可独立打开 |
| US-219 | 通过 | 侧栏金额旁与面板标题显示橙色“高峰期”；默认面板可直接看到操作按钮，价目表通过详情控件按需展开 |
| US-220 | 通过 | 中文官方表两款模型关键人民币价格断言通过；面板单位为“人民币 / 百万 tokens”，无美元或汇率文案 |
| US-221 | 通过 | 已有 Harness 会话的 AX tree 显示“输入 321K · 输出 6.5K”，侧栏与面板同步显示同一当前会话用量 |
| US-222 | 通过 | 余额面板入口和 `⇧⌘B` 均打开“HarnessDock 设置”；AX tree 显示“API 与余额”和原生安全文本栏，不再存在“完成”按钮 |
| US-223 | 通过 | 环境变量已配置时 AX tree 无安全文本栏和保存按钮；点击“更换 API Key”后才出现空安全文本栏，取消后恢复摘要 |
| US-224 | 通过 | 首次设置页自动显示 1/6 引导；可翻至 6/6，顶部入口可重开，稍后与 Escape 关闭后设置页均保持打开 |
| US-225 | 通过 | 侧栏主题入口与 `⇧⌘T` 直达统一设置的主题栏目；`⇧⌘B` 切回 API 栏目，旧 sheet 与完成按钮已移除 |
| US-226 | 通过 | 隔离回归断言组合输入第一次 Enter 发送计数 0、第二次 Enter 发送计数 1；最新版应用逐键触发系统中文输入法候选后，第一次保留 `hello` 草稿，第二次创建 `hello` 消息 |
| US-227 | 通过 | Settings 三语言选项即时切换；英文应用菜单、主题/余额注入控件和余额详情 AX tree 通过；重启后保持英文并已恢复跟随系统；官方 Harness/Chat 页面未重载或改写语言 |

## 验证结果

- `./scripts/run_checks.sh`：通过。
- `./scripts/build_app.sh`：通过。
- `plutil -lint`、`codesign --verify --deep --strict`：通过。
- 双语切换检查：English 设置页、应用菜单、`Theme Background`、`Peak`、`Current Session Tokens`、`View Model Pricing`、`Configure API Key` 和 `Refresh` 均有 AX tree 证据；重启后 `Main Interface` 保持英文，最后恢复“跟随系统”。
- 本地化边界检查：切换前后保持同一 Harness 工作区与会话；只更新原生 SwiftUI/AppKit 文案及应用注入辅助 UI，未调用第三方网页语言接口。
- Computer Use 实机检查：单层 Harness、余额胶囊及详情展开均通过。
- 增量实机检查：关闭按钮收起成功；配置动作打开安全文本栏；未读取或填写用户凭据。
- 侧栏联动检查：折叠态 AX tree 与截图通过；最终展开态截图复查遇到 ScreenCaptureKit `-3811`，但展开状态判断沿用已验证的“收起侧边栏”可访问名称。
- 对齐检查：历史本地截图曾验证紧凑余额按钮与官方鲸鱼、新会话、搜索和设置图标共用中心线；公开仓库中的旧品牌/真实工作区截图已在 HarnessDock 重命名时移除。
- 展开态融合检查：Computer Use 实机截图显示余额默认无边框、阴影或实体底色，并与设置行共用内容轴。
- 展开态图标网格检查：Computer Use 实机截图显示 `¥` 与设置齿轮共用图标中心线，`¥6.51` 与“设置”共用文字起点；状态点不再参与横向排版。
- 发布安全检查：常见密钥/私钥模式无命中；Git 确认 `.smoke-npm-cache/`、`.test-dsh-home/`、`.build/` 和 `dist/` 均被忽略；release 构建、plist 与 codesign 校验通过。
- 欢迎页入口检查：AX tree 显示“按钮 进入”，说明和功能卡不再提示必须先选择项目。
- 直接进入检查：独立测试副本首次点击“进入”后直接显示 Harness，AX tree 无 `open-panel`；`Application Support/app.dsharness.desktop/Workspace` 存在且成为最近工作区。
- 顶部切换器检查：release 构建和签名通过；实机显示紧凑等宽布局，AX tree 在当前选项上标记“已选择”，Harness 与 Chat 双向点击均通过。
- 折叠栏图标检查：主题与余额使用 24pt 图标画布并右移 2pt；最终实机折叠截图显示主题、余额和设置图标中心线一致，余额使用 18pt 带 `¥` 圆形轮廓。
- 峰谷计价核心检查：覆盖工作日 UTC 01:00/04:00/06:00/10:00 边界、周末谷时和周五至周一的下一切换；中文官方表两款 V4 模型关键人民币价格断言通过。
- 峰谷价格实机检查：侧栏摘要显示“当前高峰”；面板显示“周一 12:00 切换为谷时”、北京时间规则、两款模型两档人民币价格和中文官方价格入口；当前高峰行视觉高亮。
- 首次注入竞态检查：余额 payload 会先写入 pending，再由注入脚本就绪后应用；独立测试副本首次加载即可显示时段和余额摘要。
- 余额浮层避让检查：展开态使用 94px 底部偏移并限制视口高度，紧凑态使用 86px 偏移；浮层 `z-index: 2`。实机截图显示操作区与主题入口分离，点击刷新没有误触主题背景。
- 计价信息渐进展示检查：余额摘要可访问名称包含“高峰期”，默认面板 AX tree 不包含模型明细；“查看模型价格”显示“人民币 / 百万 tokens”，展开后提供两款模型峰谷价格与中文官方入口。
- 当前会话用量检查：进入已有 Harness 会话后，官方统计为“输入 321K tok · 输出 6.5K tok”；余额侧栏可访问名称与展开面板同步显示“输入 321K · 输出 6.5K”。
- 用量边界检查：仅复用当前页面的 Harness 会话统计，不新增远程请求；无统计时明确显示“暂无用量”。
- 原生设置检查：余额面板“配置 API Key”与 `⇧⌘B` 均打开同一个 SwiftUI Settings 窗口；窗口显示“设置 / API 与余额”、原生安全文本栏、刷新和保存动作，无独立 sheet 的“完成”按钮。
- 凭据边界检查：设置内容仍使用原生 `SecureField` 与 Keychain 方法；Harness WebView 消息只发送打开设置的动作，不承载 API Key 值。
- 已配置状态检查：真实环境变量路径显示“已配置 · 环境变量”“更换 API Key”和“刷新余额”，默认 AX tree 不包含 `SecureField` 或“保存并刷新”。
- 更换与首次配置检查：点击“更换 API Key”后出现空安全文本栏、“取消更换”和保存动作；取消后立即收起。隔离无环境变量副本仍直接显示首次输入框，刷新与保存按钮按凭据/输入状态禁用。
- 设置引导首次路径：使用独立版本的本地已读标记重启应用，首次打开官方设置后 AX tree 自动显示“1 / 6 · 先认识这页”；交付构建升级为 `dsh-settings-guide-seen-v3`，确保用户仍能亲自看到一次。
- 设置引导完整路径：方向键翻至“6 / 6 · 左侧其他入口”，AX tree 显示模型、插件、Agent 预设说明与“完成”；顶部“新手引导”可重复打开。
- 设置引导关闭检查：实机验证“稍后再看”和 Escape 只关闭引导，后方设置页与当前设置值保持原样；脚本未调用 Harness 设置 API。
- 主题设置迁移检查：Harness 左侧栏“主题背景”打开“HarnessDock 设置”，AX tree 标记“主题背景，已选择”，并显示预览、选择图片、62% 遮罩与本地存储说明。
- 定向入口检查：同一设置窗口中 `⇧⌘T` 选择主题栏目，`⇧⌘B` 选择 API 与余额栏目；手动点击两个栏目也能原地切换。
- 状态边界检查：实机验证期间未选择、移除图片或调整滑杆；源码已移除 `showThemeSettings`、独立 sheet 和主题页“完成”按钮，既有存储逻辑保持不变。
- Chat 回车旧检查（证据作废）：只在输入空白后按普通 Enter，再输入 `second`；空白本就可能被网页拒绝发送，未覆盖用户报告的非空正文路径。
- Chat 输入边界最终结论：组合态 Enter 只确认输入法并隔离网页发送逻辑；组合结束后的无修饰普通 Enter 放行给 DeepSeek 发送；四类修饰键与单行输入继续拦截，空白输入交由网页保持不发送。
- Chat 全量回归：`node scripts/check-chat-enter.mjs` 直接使用生产 user script，模拟 `insertCompositionText`、组合确认与页面发送监听器；第一次 Enter 发送计数为 0、第二次普通 Enter 发送计数为 1，四类修饰键和单行发送计数为 0，空白输入到达页面但不产生消息，非编辑按钮保持原样。
- Chat 实机输入法回归：在最新 `dist/HarnessDock.app` 的 DeepSeek Chat 编辑器中切换中文输入法，键入英文 `hello` 后按 Enter；英文由输入法确认并继续留在草稿中，编辑器未新增换行，页面未产生新消息。验证后已清空草稿并恢复原输入法。
- Chat 两次 Enter 实机回归：逐键输入 `hello` 并确认系统候选框已出现；第一次 Enter 后输入框仍保留 `hello` 且没有新消息，第二次 Enter 后输入框清空并创建 `hello` 消息。测试结束后已恢复输入法。
- Chat 真实页面复验：重启最新 release 构建后输入 `draft`，普通 Enter 后 AX 值为两行；Shift/Command/Option/Control+Enter 后草稿继续保留且页面未进入会话，随后已清空全部测试草稿。

## 完成度更新

| 项目 | 更新前 | 更新后 | 证据 |
| --- | ---: | ---: | --- |
| 去外壳化 | 0% | 100% | 构建与实机证据 |
| 余额交互收口 | 0% | 100% | AX tree 与安全配置页实机证据 |
| 侧栏折叠联动 | 0% | 100% | 折叠态实机证据与构建检查 |
| 紧凑图标对齐 | 0% | 100% | 实机截图与折叠态 6pt 样式规则 |
| 展开态视觉融合 | 0% | 100% | 实机截图与无框侧栏行样式 |
| 公开发布安全收口 | 0% | 100% | Key 继承修复、Git 忽略检查与发布审核文档 |
| 展开态图标网格 | 0% | 100% | 实机截图与固定 18pt 图标列 |
| 欢迎页入口精简 | 0% | 100% | 独立测试副本 AX tree |
| 欢迎页直接进入 | 0% | 100% | 无目录选择器的实机点击与默认工作区检查 |
| 顶部模式切换器 | 0% | 100% | release 构建、实机截图与双向切换检查 |
| 折叠栏自定义图标统一 | 0% | 100% | release 构建、AX tree 与折叠态实机截图 |
| API 峰谷计价展示 | 0% | 100% | 官方价格核验、核心边界检查、release 构建与实机面板截图 |
| 余额浮层避让 | 0% | 100% | release 构建、AX tree、实机截图与刷新/主题入口点击 |
| 计价信息渐进展示 | 0% | 100% | release 构建、AX tree、默认态截图与详情展开交互 |
| 官方人民币价格展示 | 0% | 100% | 中文官方价格页核验、核心断言与实机面板单位 |
| 当前会话 token 用量 | 0% | 100% | 已有会话 AX tree、侧栏与面板同步结果 |
| API Key 并入应用设置 | 0% | 100% | release 构建、AX tree、余额入口与快捷键实机检查 |
| 已配置凭据的设置页精简 | 0% | 100% | 环境变量、主动更换和无 Key 三条实机路径 |
| Harness 设置新手引导 | 0% | 100% | release 构建、首次自动展示、完整翻页、重复打开与关闭实机检查 |
| 主题背景并入应用设置 | 0% | 100% | release 构建、统一窗口截图、侧栏入口与双快捷键实机检查 |
| Chat 输入法确认与正常发送 | 70% | 100% | `insertCompositionText` 隔离回归、release 构建与系统中文输入法两次 Enter 实机复验 |
| 原生界面中英文切换 | 20% | 100% | 双语资源、三选项设置、即时菜单/余额/主题更新、重启持久化与完整构建门禁 |

## 剩余工作

- 当前计划内功能已完成；公开正式版仍须完成 `docs/release-readiness.md` 中的发布阻塞项。
