# 总开发阶段文档：Harness 去外壳化

## 目标

运行态只显示一层完整 Harness 界面：官方 Web UI 直接铺满 macOS 窗口，不再被原生侧栏和原生顶部栏包裹；余额入口无缝出现在 Harness 自身左侧栏。

## 非目标

- 不重写官方 Harness 的会话、导航或编辑器。
- 不修改服务启动、端口、进程树和工作区生命周期。
- 不向 Harness 注入 API Key；余额继续由原生 Keychain/API 客户端读取。

## 当前证据

- 用户截图显示原生 236pt 侧栏、原生 52pt 顶栏与完整官方 Harness UI 同时存在，形成明显的“框里套框”。
- `ContentView.swift` 运行态由 `SidebarView + WorkspaceView + HarnessWebView` 三层组成。
- `HarnessWebView.swift` 已有 WKWebView 协调器，可安全增加受控脚本和消息处理。

## 假设

- “不要框里面还有框”解释为：运行态以官方 Harness 为唯一主界面；原生欢迎、启动和错误页仍可在服务未就绪时使用。
- 余额作为轻量侧栏行嵌入官方左侧栏底部；默认无卡片边框和阴影，详情面板才使用浮层。

## 风险

| 风险 | 影响 | 缓解方案 | 负责人 |
| --- | --- | --- | --- |
| 上游 DOM 结构变化 | 余额入口位置变化 | 注入固定、独立、幂等节点，不依赖 React 类名 | Codex |
| 注入内容覆盖官方设置 | 阻挡操作 | 使用 204px 胶囊并放在设置项上方；保留指针事件范围 | Codex |
| SwiftUI 更新重建 WebView | 会话丢失 | 只更新既有 WKWebView 中的注入节点 | Codex |
| 原生入口消失 | 项目/日志难以访问 | 增加 macOS Harness 菜单与快捷键 | Codex |

## 阶段地图

| 阶段 | 目的 | 子任务 | 进入条件 | 退出条件 | 状态 |
| --- | --- | --- | --- | --- | --- |
| S1 | 定义单层运行态 | ST-201 | 截图和代码路径已确认 | 用户故事与方案明确 | 已完成 |
| S2 | 全窗显示与余额注入 | ST-202、ST-203 | 文档门禁完成 | 构建通过且运行态只有一个主框 | 已完成 |
| S3 | 可达性与验证 | ST-204、ST-205 | S2 完成 | 菜单、截图、审核通过 | 已完成 |
| S4 | 余额交互收口 | ST-206、ST-207 | 用户实机反馈 | 面板可明确收起且 API Key 入口直观 | 已完成 |
| S5 | 侧栏折叠联动 | ST-208 | 用户折叠侧栏截图 | 余额宽度与官方侧栏状态同步 | 已完成 |
| S6 | 紧凑图标对齐 | ST-209 | 132×1574 局部截图 | 余额与官方图标中心线一致 | 已完成 |
| S7 | 展开态视觉融合 | ST-210 | 用户展开态局部截图 | 余额入口与官方设置行属于同一视觉层级 | 已完成 |
| S8 | 公开发布安全收口 | ST-211 | 本地仓库与启动链路审计 | 测试目录不会误提交，余额环境变量不传给子进程 | 已完成 |
| S9 | 展开态图标网格对齐 | ST-212 | 最新实机截图 | 余额与设置使用相同图标列和文字起点 | 已完成 |
| S10 | 欢迎页入口精简 | ST-213 | 用户欢迎页截图反馈 | 首次工作区入口显示简洁的“进入” | 已完成 |
| S11 | 欢迎页直接进入 | ST-214 | 用户反馈“进入”仍弹文件夹选择器 | 自动使用应用专属默认工作区，不再弹窗 | 已完成 |
| S12 | 顶部模式切换器优化 | ST-215 | 原生分段控件留白不均、选项宽度视觉失衡 | 两项等宽、紧凑且选中态清晰 | 已完成 |
| S13 | 折叠栏自定义图标统一 | ST-216 | 主题与余额图标比官方图标偏小且中心偏左 | 统一图标画布、视觉尺寸和中心线 | 已完成 |
| S14 | API 峰谷计价展示 | ST-217 | 用户需要查看当前时段、峰谷时间和各模型双档价格 | 余额面板显示实时计价状态与官方模型价格 | 已完成 |
| S15 | 余额浮层避让主题入口 | ST-218 | 余额面板动作区与主题背景入口发生视觉和点击区域冲突 | 面板位于两个侧栏入口之上且保持最高交互层级 | 已完成 |
| S16 | 计价信息渐进展示 | ST-219 | 默认展开面板信息过多，当前高峰标识不够醒目 | 侧栏与面板标题突出当前时段，模型价格默认收起 | 已完成 |
| S17 | 官方人民币价格展示 | ST-220 | 用户指定 DeepSeek 中文价格表并要求移除美元符号 | 价格直接采用中文官方人民币表，不进行汇率换算 | 已完成 |
| S18 | 当前会话 token 用量 | ST-221 | 用户希望看到已经使用的 tokens | 复用 Harness 本地会话统计显示输入与输出 tokens | 已完成 |
| S19 | API Key 并入应用设置 | ST-222 | 用户希望 API Key 配置不再使用独立弹窗 | 使用原生 macOS 设置窗口承载“API 与余额”，凭据不进入 WebView | 已完成 |
| S20 | 已配置凭据的设置页精简 | ST-223 | 用户已有 API Key 时不希望继续看到空输入框 | 默认展示已配置状态，仅主动更换时显示安全输入框 | 已完成 |
| S21 | Harness 设置新手引导 | ST-224 | 用户不了解官方设置页各选项用途 | 首次进入自动讲解，顶部入口可随时重新打开 | 已完成 |
| S22 | 主题背景并入应用设置 | ST-225 | 用户希望主题背景与 API 配置统一管理 | 设置窗口提供两个栏目，所有主题入口直达主题背景 | 已完成 |
| S23 | Chat 输入法确认与正常发送 | ST-226 | 用户明确中文输入法组合态 Enter 只确认英文，结束组合后再按 Enter 正常发送 | 组合态英文上屏且不发送；非组合态普通 Enter 发送非空草稿 | 已完成 |
| S24 | 原生界面中英文切换 | ST-227 | 用户希望按钮、菜单及应用设置支持英文 | 设置提供跟随系统/简体中文/English；原生界面即时切换并持久化 | 已完成 |

## 验证策略

- `./scripts/run_checks.sh`
- `./scripts/build_app.sh`
- 实机打开已有 Harness 工作区，截图确认没有原生侧栏和顶部容器。
- 点击嵌入式余额入口，确认能打开原生安全配置页。
- 峰谷时段使用 UTC 日历单测覆盖工作日两段高峰、周末谷时与跨周下一切换时间。
- 从余额入口和菜单打开原生设置窗口，确认保存后窗口保留、钥匙串状态与余额刷新正常。
- 首次打开官方 Harness 设置页，确认引导自动出现一次；完成后可从“新手引导”按钮重复打开，且不会修改任何设置。
- 从 Harness 侧栏、应用菜单和 `⇧⌘T` 打开主题背景，确认均复用统一设置窗口并选中主题背景栏目。
- 在不联网的受控页面覆盖中文输入法组合态 `hello` + Enter、组合结束后的第二次普通 Enter、Shift/Command/Option/Control+Enter、单行输入框与空白草稿；断言第一次只上屏、第二次只发送一次，其余路径不发送。
- 构建后分别选择“简体中文”和“English”，复查主窗口、应用菜单、设置栏目、余额/主题及宠物弹窗；重启后语言偏好保持；“跟随系统”按系统首选语言解析。
