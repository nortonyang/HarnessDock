# Codex 审核报告：自定义主题背景

## 审核范围

- 规划中的 US-301 至 US-305。
- `AppModel.swift`、`ContentView.swift`、`HarnessDockApp.swift`、`HarnessWebView.swift` 的主题增量。

## 问题发现

| 严重级别 | 问题 | 证据 | 必要动作 |
| --- | --- | --- | --- |
| P2（已修复） | 顶栏“完成”在部分窗口状态下无法可靠接收鼠标事件 | 用户截图与独立测试副本复现 | 将按钮移至图片操作行，并直接关闭模型状态 |
| P1（已修复） | Harness 深层纯色布局遮挡已启用的主题图片 | 用户截图与独立测试副本复现 | 动态透明化大面积布局面，并在注入桥就绪后应用暂存 payload |
| 无未解决问题 | 未发现 P0/P1/P2 遗留 | 构建、核心检查、签名和实机交互通过 | 无 |

## 验收标准检查

| 用户故事 | 结果 | 证据 |
| --- | --- | --- |
| US-301 | 通过 | 图片只保存到 Application Support；不进入工作区或 Harness 请求 |
| US-302 | 通过 | 原生图片选择、缩放、原子保存、更换与移除路径已实现 |
| US-303 | 通过 | WebView 背景和遮罩实时更新，未配置时保持官方页面 |
| US-304 | 通过 | 侧栏入口、菜单命令与 `⇧⌘T` 均可打开设置 |
| US-305 | 通过 | “完成”直接关闭 sheet，按钮点击和 Esc 均通过实机验证 |
| US-306 | 通过 | 实机截图显示背景覆盖侧栏和工作区，输入框仍保持实体底色；新版注入按钮正常出现 |

## 验证结果

- `./scripts/build_app.sh`：通过。
- `./scripts/run_checks.sh`：通过。
- `codesign --verify --deep --strict dist/HarnessDock.app`：通过。
- `git diff --check`：通过。
- 独立测试副本：主题设置可打开，“完成”和 Esc 均可关闭，当前背景和遮罩未改变。
- 背景回归测试：主题启用后侧栏和主工作区均显示图片；Harness 深层 DOM 变化后动态重新标记布局面。

## 完成度更新

| 项目 | 更新前 | 更新后 | 证据 |
| --- | ---: | ---: | --- |
| 自定义主题背景 | 0% | 100% | US-301 至 US-306 全部通过 |
| 背景可见性回归 | 0% | 100% | US-306 实机截图与注入语法检查通过 |

## 剩余工作

- 无阻塞项；等待用户在重新打开正式应用后体验确认。
